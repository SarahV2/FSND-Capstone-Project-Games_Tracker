import React from 'react';
import { Redirect } from 'react-router-dom';

const TransitiveComponent = () => {
  return <Redirect to='/games/mygames' />;
};

export default TransitiveComponent;
